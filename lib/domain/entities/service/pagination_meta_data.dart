class PaginationMetaData {
  final int limit;
  final int pageSize;
  final int total;

  const PaginationMetaData({
    required this.limit,
    required this.pageSize,
    required this.total,
  });
}
